# 2026-09-24

## Baseline crawl health (week-one control)

- Sitemap: 89 URLs, all return 200. Every page's canonical tag matches its own
  URL. No missing canonicals.
- Locale hubs `/ar/ /fr/ /es/ /pt/ /it/ /de/ /pl/`: all 200, correct
  self-canonicals. Homepage hreflang cluster complete (7 locales + en +
  x-default).
- robots.txt: allows all, references sitemap.
- Repo (stage) vs production spot check: sitemap.xml, 22nd-of-the-month,
  /ar/, /fr/, /mysteries/joyful-1, /story, /rosary-visual-guide all match.
- QA scripts `scripts/qa/check_seo.py` and `check_language_routes.py` present
  on stage.

## Changes

- PR #51 (seo/fix-legacy-redirect-targets -> stage): recovers
  `/rosary(.html)` -> `/rosary-visual-guide` and
  `/the-22nd-of-the-month(.html)` -> `/22nd-of-the-month` with single-hop 301s
  placed before the generic `.html` rule. Both legacy URLs verified 404 before
  the fix. Awaiting owner approval to merge.

## Flagged

- GitHub Actions billing block: all workflows (QA, deploy) failing instantly
  since ~run #40 ("recent account payments have failed or your spending limit
  needs to be increased"). Deploy pipeline down until Billing & plans is fixed.
- Homepage: production serves d5e50ff byte-exact (Bing `msvalidate.01` meta
  included - it WAS committed and deployed normally). Commit 932c870
  ("Improve prayer experience, accessibility, and site QA") rewrote the
  homepage head on stage/main and dropped the Bing meta, but its deploy never
  ran. PR #54 re-adds the one meta line on top of the new copy. (An earlier
  read of this as "uncommitted live edits" was corrected; PR #53 closed.)

## Pending inputs (from owner)

- Search Console access (coverage/crawl report - evidence source for the full
  legacy-URL sweep).
- IndexNow key.
- Analytics access.
- Decision on committing the live homepage edits to the repo.

## Technical lane baseline (12:20 ET)

Checked: full production crawl of all 89 sitemap URLs (status, canonical,
title, description, h1, OG/Twitter, JSON-LD, robots meta), internal links and
images on every page, `check_language_routes.py https://marsharbel.com`
(157/157 pass), `verify_deployment.py` against production, repo QA on stage
and on PR #54.

Findings:
- All 89 URLs 200, self-canonical, one h1, full OG/Twitter set, valid
  JSON-LD, no noindex leaks, no duplicate titles or descriptions. www and
  http 301 to https apex; unknown paths return 404.
- Stage/production drift: `testimonies-baseline.js`, `testimonies.html`,
  `translate.js` differ from stage. Stage deploys for d5e50ff and 932c870
  never started (Actions billing).
- Raw stage fails `check_seo.py`, `test_seo.py` and `i18n:check`; PR #54's
  branch passes all of them.
- 43 internal link targets across 42 pages point at `.html` URLs that 301
  to the clean canonical. Tests enforce `.html`, so switching is parked for
  an owner decision.
- Structured data limited to WebPage and WebSite.
- Sitemap lastmod hand-set to 2026-09-20 on every URL.
- 7 titles over 65 chars, 5 descriptions over 165 chars (truncation only).
- PageSpeed API quota exhausted; CWV lab numbers pending.

Changes (patch sets handed to the integrator):
- PR A: `scripts/sitemap_lastmod.py` derives lastmod from each page's git
  history; `check_seo.py` enforces it; QA workflows fetch full history.
- PR B (stacked on A): BreadcrumbList in generated WebPage JSON-LD, plus
  generator fixes (authored titles and fields preserved, unmanaged heads
  skipped, idempotent output).

Pending: owner decision on clean internal links; publisher identity for an
Organization/WebSite publisher node; CWV measurement.
