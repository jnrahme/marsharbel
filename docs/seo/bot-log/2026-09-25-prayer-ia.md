# Bot log - 2026-09-25 - Prayer IA rework + site-wide reachability (prayers + places)

## Scope
Joey 2026-09-25 10:44 PM (todo-01M3DSKNF95HHY3DD2Z1NJ653W): "We need to rethink how all the prayer section is done and make the prayer searchable and reachable from anywhere like this page i dont know how to find it on the website." Parent 10:51 PM: fold the places cluster into the same ONE navigation change.

## What shipped
1. Searchable finder on /prayer-library (#find-a-prayer, directly under the hero): type-what-you're-carrying search box + 17 cards covering EVERY prayer page on the site (9 intention pages, novena, Saint Charbel Prayers, feast day, rosary guide/intro/minibook, mystery meditation, become-like-charbel), each with keywords (data-keywords) + a plain-English one-liner. prayer-finder.js (new, 30 lines, no network/storage) filters live; empty state message. Input reuses the existing .coach-grid component classes - zero CSS additions.
2. Nav (partials/primary-navigation.html, synced to all 84 pages via scripts/sync-navigation.mjs): Prayer dropdown now lists every prayer page - Library, Find a Prayer by Intention (anchor link to the finder), all 9 intention pages, then the existing entries. NEW top-level Places dropdown: Visiting Annaya, Bekaa Kafra, Saint Charbel's Lebanon, The Hermitage, Saint Charbel Trail.
3. Sibling mesh: each of the 9 intention pages gained a "Prayers for Other Needs" strip linking the other 8 + the library finder.
4. Cross-links: homepage monthly-prayer CTA gained "Find a prayer for what you're carrying"; st-rafqa / st-maroun / st-nimatullah gained a "Pray With..." section; miracles/index gained "Prayers for What You Are Carrying" (healing + miracle + finder, with the no-guaranteed-outcomes line).

## Merge-order note (important)
The Places dropdown deliberately does NOT yet link /saint-charbel-pilgrimage: that page is in the pending seo/charbel-pilgrimage bundle, and check_seo correctly failed the nav branch on 170 missing-target errors when the link was included. Every bundle must be QA-green standalone. FOLLOW-UP (queued in my next cycle, after the pilgrimage bundle merges): add "Pilgrimage Guide" as the Places parent/first item and re-run sync - mechanical one-line change.

## QA
check_i18n_policy PASS (85 html + prayer-finder.js max-merged into baseline via the policy's own extractors); check_seo 132 pages PASS; test_seo 15/15 OK; navigation sync 0 out of sync; diff --check clean. Pixel: finder filters correctly ("anxiety" -> 1 card, "baby" -> 1 card mobile), dropdown renders all 18 Prayer items + Places top-level, zero console errors. Screenshots attached to report.

IndexNow after merge: /prayer-library plus all nav-touched pages is impractical - the meaningful content changes are /prayer-library, the 9 intention pages, /, /miracles/, /st-rafqa, /st-maroun, /st-nimatullah.
