# 2026-09-25 - English reading guide prayer mirror

After the English-master/Arabic pilot (ababbc4), the static `/en/prayers` reading guide is promoted to the same complete prayer page shape, without changing its canonical URL or removing the already published route. It uses the English mirror catalog's 114 text slots and the same template, photo, seven sections, 14 prayer cards, sources and intercessions as `/saint-charbel-prayers`. Its own keyed title and description differentiate two indexable English URLs; the rest of the visible body is intentionally identical. The metadata catalog is `locales/en/mirrors/prayers-guide.json`.

The change is made through the generator and catalog. `en/prayers.html` remains generated. This leaves the five other guide topics unchanged and does not widen any locale's publication status. `/en/prayers` keeps the existing full prayer hreflang cluster and `x-default` to itself; `/saint-charbel-prayers` retains the original English master self/x-default links. The `sitemap.xml` lastmod for `/en/prayers` is updated to its actual edit date. The route continues to switch to `/ar/prayers` through the existing top language selector, and retains no second language control.

Quality gates: `i18n:check`, `i18n:test`, `check_seo`, `test_seo` and the mirror parity test. The 74-test international laptop suite and 10 dedicated phone/laptop mirror tests pass. I inspected local English-guide screenshots at 1366px and 390px; the photo/cards/section hierarchy render without horizontal overflow or broken media. Browser checks cover EN guide/AR mirror selector navigation, accessibility, one historical image, 7 sections and 14 cards at phone and desktop widths. Live verification and production screenshot pairs must follow an approved merge/deploy. The current stage still has the old guide until then. The next locale rollout is French, followed by Spanish, Portuguese, Italian, German and Polish, one complete mirror at a time.
